% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_fill_ltc}
\alias{scale_fill_ltc}
\title{ggplot2 Fill Scale from an ltc Palette}
\usage{
scale_fill_ltc(name, discrete = TRUE, direction = 1, ...)
}
\arguments{
\item{name}{Character or unquoted name. The name of the desired palette.}

\item{discrete}{\code{TRUE} for a discrete scale, \code{FALSE} for a continuous one.}

\item{direction}{1 keeps the palette order, -1 reverses it.}

\item{...}{Passed on to \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}} when \code{discrete = TRUE},
or to \code{\link[ggplot2:scale_gradient]{ggplot2::scale_fill_gradientn()}} when \code{discrete = FALSE}.}
}
\value{
A ggplot2 scale, to add to a plot.
}
\description{
Colours the \code{fill} aesthetic with an ltc palette, the way
\code{scale_fill_viridis()} does. Set \code{discrete = FALSE} for a continuous scale.
}
\examples{
library(ggplot2)

# discrete
ggplot(mtcars, aes(factor(cyl), mpg, fill = factor(cyl))) +
  geom_boxplot() +
  scale_fill_ltc(maya)

# continuous
ggplot(faithfuld, aes(waiting, eruptions, fill = density)) +
  geom_raster() +
  scale_fill_ltc(heatmap0, discrete = FALSE)
}
